name: 'grampa'
description: 'Find oldest files in Git repo'
##inputs:
##  who-to-greet:  # id of input
##    description: 'Who to greet'
##    required: true
##    default: 'World'
outputs:
  report:
    description: 'The time we greeted you'
runs:
  using: 'docker'
  image: 'Dockerfile'

